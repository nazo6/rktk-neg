{
  "version": "2.0.0",
  "options": {
    "env": {
      "DEFMT_LOG": "info"
    }
  },
  "tasks": [
    {
      "label": "deploy-master-right",
      "type": "shell",
      "command": "cargo run --release --bin master --features right,trouble"
    },
    {
      "label": "deploy-slave-left",
      "type": "shell",
      "command": "cargo run --release --bin master --features left"
    }
  ]
}
