{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "deploy-right",
      "type": "shell",
      "command": "rktk-cli build -d auto"
    },
    {
      "label": "deploy-left",
      "type": "shell",
      "command": "rktk-cli build -d auto -- --no-default-features --features left"
    },
    {
      "label": "deploy-left-slave",
      "type": "shell",
      "command": "rktk-cli build -d auto -- --no-default-features --features left,force-slave"
    },
    {
      "label": "deploy-ble-left-slave",
      "type": "shell",
      "command": "rktk-cli build -d auto -- --no-default-features --features left,ble-split-slave"
    },
    {
      "label": "deploy-ble-right-master",
      "type": "shell",
      "command": "rktk-cli build -d auto -- --no-default-features --features right,ble-split-master"
    }
  ]
}
